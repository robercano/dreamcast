#include <stdio.h>
#include <stdint.h>
#include <math.h>

int main(int argc, char **argv)
{
/*    if (argc > 1) {
        long n = strtol(argv[1], NULL, 0);
        printf("%d\n", n);
    } else {
        printf("no args\n\n");
    }
    float t = sqrt(2);
    printf("%f\n", t);*/
    printf("%d\n", 45);
    return 0;
}
